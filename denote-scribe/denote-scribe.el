;;; denote-scribe.el --- Compatibility loader for Denote Scribe -*- lexical-binding: t; -*-

;;; Code:

(load-file (expand-file-name "scripts/denote-scribe.el"
                             (file-name-directory (or load-file-name buffer-file-name))))

;;; denote-scribe.el ends here
