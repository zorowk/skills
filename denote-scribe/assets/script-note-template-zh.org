* 用途
这个脚本完成什么任务？为什么值得保留？

* 适用场景
说明脚本适用的情况，以及不应使用它的边界。

* 前置条件
- 工作目录：
- 所需工具或服务：
- 所需权限：

* 输入与副作用
- 输入和参数：
- 读取的文件、服务或状态：
- 修改的文件、服务或状态：

* 脚本
将可执行内容保留在 Org Babel 中。使用准确的语言，保留 `:eval query`，
不要添加 `:tangle`。

#+begin_src shell :eval query
set -eu
# 请将这行注释替换为可执行脚本。
#+end_src

* 运行方法
记录源码块位置、调用方法、参数和预期输出。

* 验证与恢复
- 成功标准：
- 失败信号：
- 恢复或回滚方法：

* 维护说明
记录版本假设、脆弱依赖和已知限制。

* 来源
记录对应的对话、调查、项目或外部资料。
