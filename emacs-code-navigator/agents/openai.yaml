interface:
  display_name: "Emacs Code Navigator"
  short_description: "Locate symbols and inspect live Emacs code context"
  default_prompt: "Use $emacs-code-navigator to locate code efficiently through live buffers, bounded text search, xref, and language-server symbols."
