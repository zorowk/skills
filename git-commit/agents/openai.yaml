interface:
  display_name: "Git Commit"
  short_description: "Inspect, scope, and commit evidence-backed changes"
  default_prompt: "Use $git-commit to inspect or review actual Git changes, prepare a message when requested, and commit only explicitly authorized paths."
