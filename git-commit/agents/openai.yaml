interface:
  display_name: "Git Commit"
  short_description: "Generate evidence-backed Git commit messages"
  default_prompt: "Use $git-commit to inspect current changes and create an adaptive, evidence-backed message that is concise for simple work and complete for risky work."
